/*=============================================== {{ pascalCase name }} model ===============================================*/

import { Schema, model } from "mongoose"

const {{ pascalCase name }}Schema = new Schema(
    {
        
    },
    { timestamps: true }
)

export const {{ pascalCase name }}Model = model("{{ pascalCase name }}", {{ pascalCase name }}Schema)
