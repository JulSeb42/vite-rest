/*=============================================== {{ pascalCase name }} styles ===============================================*/

import styled from "styled-components"

export const Styled{{ pascalCase name }} = styled.{{ lowerCase tag }}`
	
`
