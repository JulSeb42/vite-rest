/*=============================================== {{ pascalCase name }} types ===============================================*/

import type { {{#if as}} ElementType,{{/if}} } from "react"

export interface {{ pascalCase name }}Props {
    "data-testid"?: string
    {{#if children}}
    children?: Children
    {{/if}}
    {{#if as}}
    as?: ElementType
    {{/if}}
}
